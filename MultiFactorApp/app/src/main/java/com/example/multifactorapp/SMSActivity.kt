package com.example.multifactorapp

import android.app.Activity
import android.content.Intent
import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class PasswordActivity : AppCompatActivity() {

    /*
    To be determined what goes in here

    SMS token will be 10 tokens at random (any ascii character)

    TODO:
    find time delay in when SMS token was sent
    calculate if manuel entry, copy/paste, or sms token (like the duo click button) - bc each will have a diff time delay
    Create another app for the token

    */
}